{
  "name": "@jeffherb/json-web-signature-2020",
  "description": "A forked version of https://github.com/w3c-ccg/lds-jws2020."
  "version": "0.0.1",
  "scripts": {
    "shove": "git add -A; git commit -m 'chore(ci): test'; git push origin master",
    "postinstall": "npm run bootstrap",
    "bootstrap": "lerna bootstrap",
    "release:canary": "lerna publish --canary",
    "release": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
